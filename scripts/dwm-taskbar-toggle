#!/bin/bash
# Toggle window hide state via dwm's _DWM_TOGGLE_HIDE property
wid="$1"
[ -z "$wid" ] && exit 1

# Set _DWM_TOGGLE_HIDE on root window - dwm reads this and toggles the window
xprop -root -f _DWM_TOGGLE_HIDE 32x -set _DWM_TOGGLE_HIDE "$wid"
